<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="br.ufc.quixada.myapplicationnn">

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_seu_ticket_round"
        android:label="Seu Ticket"
        android:roundIcon="@mipmap/ic_launcher_seu_ticket_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplicationnn"
        tools:targetApi="31">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBTqw7tVuE5X9ioSgAoAt95Ygmz-Vsizq4" />

        <activity
            android:name=".Maps"
            android:exported="false"
            android:label="@string/title_activity_maps" />
        <activity
            android:name=".CrudUser.Cadastro"
            android:exported="false" />
        <activity
            android:name=".CrudEvento.CadEvento"
            android:exported="false" />
        <activity
            android:name=".CrudEvento.EditEvento"
            android:exported="false" />
        <activity
            android:name=".Compra"
            android:exported="false" />
        <activity
            android:name=".CrudUser.EditProfile"
            android:exported="false" />
        <activity
            android:name=".CrudUser.EditarSenha"
            android:exported="false" />
        <activity
            android:name=".TelaEventos"
            android:exported="false" />
        <activity
            android:name=".MainActivityHome"
            android:exported="false" />
        <activity
            android:name=".LoginST"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>